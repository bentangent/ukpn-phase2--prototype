@html.extend('layout', function(model) {
  @html.block('content', function(model) {
    @model.components.forEach(function(component) {
      @html.include('/installers-landing/partials/component.vash', component)
    })
  })
  @html.append('styles', function(model) {
    <link rel='stylesheet' href='/installers-landing/css/installers-landing.css' />
  })
  @html.append('scripts', function() {
    <script src="/installers-landing/js/installers-landing.bundle.js"></script>
  }) 
})
