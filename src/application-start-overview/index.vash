@html.extend('layout', function(model) {
  @html.block('content', function(model) {
    @model.components.forEach(function(component) {
      @html.include('/application-start-overview/partials/component.vash', component)
    })
  })
  @html.append('styles', function(model) {
    <link rel='stylesheet' href='/application-start-overview/css/application-start-overview.css' />
  })
  @html.append('scripts', function() {
    <script src="/application-start-overview/js/application-start-overview.bundle.js"></script>
  }) 
})
